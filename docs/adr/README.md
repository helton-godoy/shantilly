# Architecture Decision Records

ADRs preserve decisions that future sessions must not silently reverse. Copy the structure of an existing record, use the next sequential number, and choose one status: Proposed, Accepted, Superseded, or Rejected.

An ADR is required when changing the compatibility contract, phase order, build system, component boundaries, public API, or dependency strategy. Implementation details that do not affect these areas do not require an ADR.
